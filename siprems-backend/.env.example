# Database Configuration
DB_HOST=localhost
DB_NAME=siprems_db
DB_USER=postgres
DB_PASSWORD=mysecretpassword
DB_PORT=5432

# Flask Configuration
FLASK_ENV=development

# JWT Configuration
JWT_SECRET_KEY=change-this-to-a-secure-random-key-in-production-min-32-chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting Configuration
RATELIMIT_STORAGE_URL=memory://
RATELIMIT_DEFAULT=100/hour

# Celery & Redis Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
REDIS_PORT=6379
REDIS_PASSWORD=

# Model Storage Configuration
MODELS_DIR=/app/models

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Production Notes:
# - Change FLASK_ENV to 'production'
# - Set JWT_SECRET_KEY to a strong random value (min 32 characters)
# - Set DB_PASSWORD to a strong password
# - Update CORS_ALLOWED_ORIGINS to your actual domain
# - Set RATELIMIT_STORAGE_URL to redis://your-redis-host:6379 for production
# - Set CELERY_BROKER_URL to your Redis instance (redis://redis:6379/0 in Docker)
# - Set MODELS_DIR to a persistent volume path (/app/models in Docker)
# - Consider using separate Redis instances for broker, result backend, and rate limiting in production
